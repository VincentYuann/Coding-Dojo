import AnimeCard from '../components/AnimeCard';
import { animeSeasons } from '../services/animeService'

function UpcomingSeasons() {
    return (
        <div>
            <h1>Upcoming Seasons</h1>
            <p>Stay tuned for the latest updates on upcoming seasons!</p>
        </div>
    );
}

export default UpcomingSeasons;